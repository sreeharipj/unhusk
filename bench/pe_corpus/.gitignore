# Cross-compiled binaries/PDBs (2.7G) and build logs -- regenerated by
# build.sh, not the deliverable. Kept: build.sh, analyze.py, rows.json
# (the per-function measurement), analysis.json (the aggregated numbers),
# REPORT.md -- matching bench/hypotheses/.gitignore's convention.
out/
*.log
__pycache__/
*.pyc
