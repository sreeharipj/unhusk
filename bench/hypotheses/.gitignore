# Bulky/intermediate build products and raw dumps, not the deliverable.
# Kept: *.py, *.sh, *_output.json, *_output.md, *_sweep.png, *_curve.png --
# the scripts and their numeric results, matching bench/rulemine/.gitignore's
# convention (derived binaries/raw dumps are regenerated by the build
# scripts here, not tracked).
v_pe/
v_inline_suppressed/
v_cgu_sweep/
h3_1_functions.tsv
h3_1_raw_results.json
__pycache__/
*.pyc
